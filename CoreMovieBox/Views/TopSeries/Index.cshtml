@using EntityLayer.Concrete
@model List<Top10TvSerieList>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>Image</th>
        <th>Sil</th>
        <th>Güncelle</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.TvSerieID</td>
            <td><img src="@item.Image" style="width: 100px; height: 100px;" /></td>
            <td><a href="/TopSeries/DeleteTopSerie/@item.TvSerieID" class="btn btn-outline-danger">Sil</a></td>
            <td><a href="/TopSeries/UpdateTopSerie/@item.TvSerieID" class="btn btn-outline-success">Güncelle</a></td>
        </tr>
    }
</table>
<a href="/TopSeries/AddTopSerie" class="btn btn-primary">Yeni Film Girişi</a>

